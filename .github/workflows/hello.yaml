name: Hello World

on: 
    push:
        branches: 
            - main

jobs:
    hello-world-job:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run:
                echo "Hello World!"
  